# ============================================================
# requirements.txt  —  Telegram Automation Engine v4 (main.py)
# Python 3.9+  |  pip install -r requirements.txt
# Tested together: no conflicts (pip check clean)
# ============================================================

# ---------------- Web Framework ----------------
fastapi>=0.115,<1.0            # /  /health endpoints + lifespan
uvicorn[standard]>=0.30,<1.0   # ASGI server  (start: uvicorn main:app --host 0.0.0.0 --port $PORT)

# ---------------- Database ----------------
motor>=3.5,<4.0                # Async MongoDB driver (main.py uses this)
pymongo[srv]>=4.8,<5.0         # [srv] = dnspython -> ZAROORI for mongodb+srv:// URIs
                               # (bina iske Render pe "dnspython must be installed" error)

# ---------------- Telegram ----------------
telethon>=1.34,<2.0            # MTProto client (StringSession, InviteToChannel, etc.)
cryptg>=0.4,<1.0               # Optional: faster AES for Telethon (harvester speed-up)

# ---------------- Utilities ----------------
pytz>=2024.1                   # IST / UTC timestamps

# ---------------- Local dev only (Render pe zaroorat nahi) ----------------
python-dotenv>=1.0,<2.0        # .env load for local testing

# ---------------- NOT needed by current main.py (proxy removed) ----------------
# Uncomment ONLY if you re-add proxy_pool.py / SOCKS proxies:
# httpx[socks]>=0.27,<1.0
# PySocks>=1.7,<2.0
# pydantic>=2.7,<3.0           # FastAPI already installs this automatically
