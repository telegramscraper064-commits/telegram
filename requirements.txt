# ============================================================
# requirements.txt  —  Web + DB + Proxy-Pool + Telegram stack
# Python 3.9+ compatible | Install: pip install -r requirements.txt
# Versions pinned to stable ranges (compatible set, no conflicts)
# ============================================================

# ---------------- Web Framework ----------------
fastapi>=0.115,<1.0            # Admin endpoints + health checks (ASGI)
uvicorn[standard]>=0.30,<1.0   # ASGI server to run FastAPI
pydantic>=2.7,<3.0             # Config/settings models (FastAPI dependency)

# ---------------- Database ----------------
motor>=3.5,<4.0                # Async MongoDB driver (FastAPI / asyncio code)
pymongo>=4.8,<5.0              # Sync MongoDB driver (scripts / migrations / CLI)

# ---------------- Telegram Client ----------------
telethon>=1.34,<2.0            # MTProto client for Telegram automation
cryptg>=0.4,<1.0               # OPTIONAL but recommended: speeds up crypto (Telethon)

# ---------------- Networking / Proxy ----------------
httpx[socks]>=0.27,<1.0        # Async HTTP client + SOCKS5 support
                               # ([socks] extra pulls in `socksio` —
                               #  needed by proxy_pool.py for socks5:// URLs)

PySocks>=1.7,<2.0              # SOCKS5 proxy support for Telethon (tuple-style)
                               # Required because Telethon's `proxy` parameter
                               # expects (socks.SOCKS5, ip, port, True, user, pwd)

# ---------------- Utilities ----------------
python-dotenv>=1.0,<2.0        # Load .env vars in local dev
pytz>=2024.1                   # IST timezone handling
                               # (Python 3.9+ also has stdlib `zoneinfo` if you
                               #  want to drop this dependency later)

# ---------------- Optional ----------------
# loguru>=0.7,<1.0             # OPTIONAL: structured logging (stdlib logging is enough)
# apscheduler>=3.10,<4.0       # OPTIONAL: background scheduling (you use asyncio.sleep)
